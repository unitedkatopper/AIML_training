# Notebook Folder

This folder is added to match the internship submission structure.
If required, place your Jupyter notebook for EDA/model experiments here.
For this project, EDA is available through `eda.py` and training through `train.py`.
